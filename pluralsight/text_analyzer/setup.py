from distutils.core import setup

setup(
    name='text_analyzer',
    version='1.0.0',
    py_modules=['text_analyzer'],

    # metadata
    author='Vasyl Velmyk',
    description='some usefull description'
)
